/*import { TestBed, inject } from '@angular/core/testing';
import { ExceptionTransService } from '../../exceptiontrans.service';
import { HttpClientModule } from '@angular/common/http';

describe('ExceptionTransService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: [HttpClientModule],
      providers: [ExceptionTransService]
    });
  });

  it('ExceptionTransService should be created', inject([ExceptionTransService], (service: ExceptionTransService) => {
    expect(service).toBeTruthy();
  }));
});*/
